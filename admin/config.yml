# Decap CMS configuration for dietchennai.org
# Structured content (announcements, circulars, downloads, banner images,
# the last-updated date, and the photo gallery) lives in small JSON data
# files under assets/content/data/ and assets/content/gallery/, fetched and
# rendered at runtime by /js/cms-content.js. Editing those collections below
# is safe and will never break page layout.
#
# As of the Phase 2 Eleventy migration, every other page's content lives as
# an Eleventy template under src/<page>/index.njk (src/index.njk for the
# homepage, src/404.njk for 404), built to the page actually served on
# dietchennai.org. The shared header, navigation, and footer are NOT part of
# any individual page file any more -- they live once in src/_includes/ and
# apply to every page automatically, so editing a page here can no longer
# break the site-wide nav or footer the way editing the old flat HTML files
# could. Each entry in "All Website Pages" below points at the page's
# src/*.njk source (bottom of this file) as a raw-text fallback for content
# not covered by a structured collection above -- use carefully, and prefer
# the structured collections when the content you want to change is an
# announcement, circular, document, banner image, or the last-updated date.
# This CMS/admin pipeline is intended for dietchennai.org only -- the
# diet.ac.in mirror is a read-only replica and blocks /admin at Cloudflare.

backend:
  name: github
  repo: rvkmar/dietwebsite
  branch: main # Change to master if your default branch is master
  site_domain: dietchennai.org # Directs Decap to publish cleanly to your custom domain

# Media File Paths (default — used by the raw HTML pages and anywhere a
# field below doesn't override it)
media_folder: "images"
public_folder: "/images"

collections:
  - name: "site_settings"
    label: "Site Settings"
    files:
      - name: "settings"
        label: "General Settings"
        file: "assets/content/data/site-settings.json"
        description: "Controls the 'Last Updated' date shown in the footer of every page."
        fields:
          - { label: "Last Updated (e.g. 18-APR-2025)", name: "last_updated", widget: "string" }

  - name: "announcements"
    label: "Announcements"
    files:
      - name: "announcements"
        label: "Announcements List"
        file: "assets/content/data/announcements.json"
        description: "Shown on the homepage 'Announcements' tab and on the /announcements/ page."
        fields:
          - label: "Announcements"
            name: "items"
            widget: "list"
            summary: "{{title}} — {{date}}"
            fields:
              - { label: "Title", name: "title", widget: "string" }
              - { label: "Date", name: "date", widget: "date", date_format: "YYYY-MM-DD", required: false }
              - { label: "External Link (optional)", name: "link", widget: "string", required: false }
              - label: "Attach Document (optional)"
                name: "file"
                widget: "file"
                required: false
                media_folder: "/assets/docs"
                public_folder: "/assets/docs"

  - name: "circulars"
    label: "Circulars"
    files:
      - name: "circulars"
        label: "Circulars List"
        file: "assets/content/data/circulars.json"
        description: "Shown on the homepage 'Circulars' tab and on the /circulars/ page."
        fields:
          - label: "Circulars"
            name: "items"
            widget: "list"
            summary: "{{title}} — {{date}}"
            fields:
              - { label: "Title", name: "title", widget: "string" }
              - { label: "Date", name: "date", widget: "date", date_format: "YYYY-MM-DD", required: false }
              - { label: "External Link (optional)", name: "link", widget: "string", required: false }
              - label: "Attach Document (optional)"
                name: "file"
                widget: "file"
                required: false
                media_folder: "/assets/docs"
                public_folder: "/assets/docs"

  - name: "downloads"
    label: "Downloads / Documents"
    files:
      - name: "downloads"
        label: "Downloads, Forms, Publications & Reports"
        file: "assets/content/data/downloads.json"
        description: "Shown on the homepage 'Downloads' tab and on /downloads/, /doc-forms/, /doc-publications/, /reports/. Use Category to route an entry to the right page."
        fields:
          - label: "Documents"
            name: "items"
            widget: "list"
            summary: "{{title}} ({{category}})"
            fields:
              - { label: "Title", name: "title", widget: "string" }
              - { label: "Date", name: "date", widget: "date", date_format: "YYYY-MM-DD", required: false }
              - label: "Category"
                name: "category"
                widget: "select"
                options: ["forms", "publications", "reports"]
                hint: "Determines whether this appears on /doc-forms/, /doc-publications/, or /reports/. It always also appears on /downloads/."
              - { label: "External Link (optional)", name: "link", widget: "string", required: false }
              - label: "Attach Document (optional)"
                name: "file"
                widget: "file"
                required: false
                media_folder: "/assets/docs"
                public_folder: "/assets/docs"

  - name: "banner"
    label: "Homepage Banner"
    files:
      - name: "banner"
        label: "Banner Slides"
        file: "assets/content/data/banner.json"
        description: "Images shown in the rotating banner at the top of the homepage."
        fields:
          - label: "Slides"
            name: "items"
            widget: "list"
            summary: "{{alt}}"
            fields:
              - label: "Image (English / default)"
                name: "image"
                widget: "image"
                media_folder: "/images/banner"
                public_folder: "/images/banner"
              - label: "Image (Tamil version, optional)"
                name: "image_ta"
                widget: "image"
                required: false
                media_folder: "/images/banner"
                public_folder: "/images/banner"
                hint: "If set, this replaces the English image above when a visitor switches the site to Tamil."
              - { label: "Alt Text", name: "alt", widget: "string", required: false }

  - name: "gallery"
    label: "Photo Gallery"
    files:
      - name: "gallery"
        label: "Gallery Images"
        file: "assets/content/gallery/gallery.json"
        description: "Photos shown on the /gallery/ page."
        fields:
          - label: "Photos"
            name: "images"
            widget: "list"
            field:
              label: "Photo"
              name: "image"
              widget: "image"
              media_folder: "/assets/content/gallery"
              public_folder: "/assets/content/gallery"

  - name: "academic_faculty"
    label: "Academic Faculty"
    files:
      - name: "academic_faculty"
        label: "Academic Faculty List"
        file: "assets/content/data/academic-faculty.json"
        description: "Shown on the /academic-faculty/ page (flip-cards with photo, name, designation, qualification, experience, email, phone). Reorder entries here to reorder the page; delete an entry to remove someone; add one for a new hire. The Principal is NOT listed here — edit the Principal collection below instead; their card is added to this page automatically."
        fields:
          - label: "Faculty Members"
            name: "items"
            widget: "list"
            summary: "{{name}} — {{designation}}"
            fields:
              - label: "Photo"
                name: "photo"
                widget: "image"
                media_folder: "/assets/staff"
                public_folder: "/assets/staff"
              - { label: "Name", name: "name", widget: "string" }
              - { label: "Designation", name: "designation", widget: "string" }
              - { label: "Qualification", name: "qualification", widget: "text", required: false }
              - { label: "Teaching Experience", name: "experience", widget: "string", required: false, hint: "e.g. '24 years'" }
              - { label: "Email", name: "email", widget: "string", required: false }
              - { label: "Phone", name: "phone", widget: "string", required: false }

  - name: "principal"
    label: "Principal"
    files:
      - name: "principal_profile"
        label: "Principal's Photo & Profile"
        file: "assets/content/data/principal.json"
        description: "The Principal's photo, name, and designation — used on the Academic Faculty card, the homepage, the Principal's Desk page, and the Mandatory Disclosure document. Edit it here once and it updates everywhere; no other collection controls this anymore."
        fields:
          - label: "Photo"
            name: "photo"
            widget: "image"
            media_folder: "/images"
            public_folder: "/images"
          - { label: "Name", name: "name", widget: "string", hint: "Used everywhere: this card, the homepage, Principal's Desk page, and the Mandatory Disclosure document." }
          - { label: "Name (Tamil)", name: "name_ta", widget: "string", required: false, hint: "Shown on the homepage and Principal's Desk page when a visitor switches to Tamil. Leave blank to just keep showing the English name in Tamil mode too." }
          - { label: "Designation (short, for this card)", name: "designation", widget: "string", hint: "Shown only on this Academic Faculty card, e.g. 'Principal' \u2014 matches the style of every other faculty member's card." }
          - { label: "Designation (full title, for homepage/desk/disclosure)", name: "designation_long", widget: "string", hint: "e.g. 'Principal, DIET Chennai' \u2014 shown on the homepage, Principal's Desk page, and the Mandatory Disclosure document." }
          - { label: "Designation (full title, Tamil)", name: "designation_long_ta", widget: "string", required: false }
          - { label: "Qualification", name: "qualification", widget: "text", required: false }
          - { label: "Teaching Experience", name: "experience", widget: "string", required: false }
          - { label: "Email", name: "email", widget: "string", required: false }
          - { label: "Phone", name: "phone", widget: "string", required: false }
      # Note: the Principal's Name & Designation (principalname /
      # principaldesignation keys, English + Tamil) are edited via the
      # "Site Translations" collection instead of here, since that
      # collection now covers every key in languages/en.json and
      # languages/ta.json, including these two.
      - name: "principal_disclosure"
        label: "Mandatory Disclosure Document (raw HTML)"
        file: "assets/docs/mandatory_disclosure.html"
        description: "The Principal's name appears TWICE in this legal/RTI document (as a listed detail and as a signature line) and is plain static text, not linked to the fields above — you must update both occurrences yourself if the Principal changes."
        fields:
          - { label: "Full page HTML source", name: "body", widget: "code", default_language: "html", allow_language_selection: false, output_code_only: true }

  - name: "admin_staff"
    label: "Non-Academic Staff"
    files:
      - name: "admin_staff"
        label: "Non-Academic Staff List"
        file: "assets/content/data/admin-staff.json"
        description: "Shown on the /admin-staff/ page (flip-cards with photo, name, designation, qualification, experience, email, phone). Reorder entries here to reorder the page; delete an entry to remove someone; add one for a new hire."
        fields:
          - label: "Staff Members"
            name: "items"
            widget: "list"
            summary: "{{name}} — {{designation}}"
            fields:
              - label: "Photo"
                name: "photo"
                widget: "image"
                media_folder: "/assets/staff"
                public_folder: "/assets/staff"
              - { label: "Name", name: "name", widget: "string" }
              - { label: "Designation", name: "designation", widget: "string" }
              - { label: "Qualification", name: "qualification", widget: "text", required: false }
              - { label: "Experience", name: "experience", widget: "string", required: false, hint: "e.g. '9 years'" }
              - { label: "Email", name: "email", widget: "string", required: false }
              - { label: "Phone", name: "phone", widget: "string", required: false }

  - name: "about_fragments"
    label: "About / Roles Content"
    files:
      - name: "about_mission"
        label: "Mission & Vision content (embedded on /mission-vision/)"
        file: "assets/content/about_mission.html"
        fields:
          - { label: "Full page HTML source", name: "body", widget: "code", default_language: "html", allow_language_selection: false, output_code_only: true }
      - name: "about_roles"
        label: "Roles & Functions content (embedded on /roles-functions/)"
        file: "assets/content/about_roles.html"
        fields:
          - { label: "Full page HTML source", name: "body", widget: "code", default_language: "html", allow_language_selection: false, output_code_only: true }

  - name: "translations"
    label: "Site Translations"
    files:
      - name: "strings_en"
        label: "All Site Text (English)"
        file: "languages/en.json"
        description: "Every piece of translatable text on the site (nav labels, headings, button text, the Principal's name/designation, etc.) — 83 keys, all shown here. Edit the Tamil equivalent of each one in 'All Site Text (Tamil)' below; each field there shows the current English text as a hint so you can match them up."
        fields:
            - label: home
              name: home
              widget: text
            - label: government
              name: government
              widget: text
            - label: scert
              name: scert
              widget: text
            - label: address
              name: address
              widget: text
            - label: skip
              name: skip
              widget: text
            - label: search
              name: search
              widget: text
            - label: web
              name: web
              widget: text
            - label: diet
              name: diet
              widget: text
            - label: more
              name: more
              widget: text
            - label: aboutus
              name: aboutus
              widget: text
            - label: aboutdiet
              name: aboutdiet
              widget: text
            - label: introduction
              name: introduction
              widget: text
            - label: missionvision
              name: missionvision
              widget: text
            - label: rolefunction
              name: rolefunction
              widget: text
            - label: organization
              name: organization
              widget: text
            - label: districtprofile
              name: districtprofile
              widget: text
            - label: districtstatistics
              name: districtstatistics
              widget: text
            - label: principalsdesk
              name: principalsdesk
              widget: text
            - label: staff
              name: staff
              widget: text
            - label: academicstaff
              name: academicstaff
              widget: text
            - label: adminstaff
              name: adminstaff
              widget: text
            - label: courses
              name: courses
              widget: text
            - label: coursedeled
              name: coursedeled
              widget: text
            - label: coursetpd
              name: coursetpd
              widget: text
            - label: departments
              name: departments
              widget: text
            - label: pste
              name: pste
              widget: text
            - label: tpd
              name: tpd
              widget: text
            - label: etmd
              name: etmd
              widget: text
            - label: ae
              name: ae
              widget: text
            - label: fiar
              name: fiar
              widget: text
            - label: library
              name: library
              widget: text
            - label: actsrules
              name: actsrules
              widget: text
            - label: rti
              name: rti
              widget: text
            - label: dietrules
              name: dietrules
              widget: text
            - label: activities
              name: activities
              widget: text
            - label: calendar
              name: calendar
              widget: text
            - label: current_activities
              name: current_activities
              widget: text
            - label: activity_archive
              name: activity_archive
              widget: text
            - label: alumni
              name: alumni
              widget: text
            - label: textbookmodules
              name: textbookmodules
              widget: text
            - label: textbook
              name: textbook
              widget: text
            - label: modules
              name: modules
              widget: text
            - label: contact-us
              name: contact-us
              widget: text
            - label: documents
              name: documents
              widget: text
            - label: publications
              name: publications
              widget: text
            - label: reports
              name: reports
              widget: text
            - label: forms
              name: forms
              widget: text
            - label: important_links
              name: important_links
              widget: text
            - label: dietfullname
              name: dietfullname
              widget: text
            - label: readmore
              name: readmore
              widget: text
            - label: principalname
              name: principalname
              widget: text
              hint: This value is NOT read anywhere on the site anymore \u2014 the Principal's name is now controlled entirely by the Principal collection, so it stays in sync across every page. Left here only so nothing in the translation file goes unmapped.
            - label: principaldesignation
              name: principaldesignation
              widget: text
              hint: This value is NOT read anywhere on the site anymore \u2014 use the Principal collection's "Designation (full title...)" field instead.
            - label: announcements
              name: announcements
              widget: text
            - label: circulars
              name: circulars
              widget: text
            - label: media
              name: media
              widget: text
            - label: downloads
              name: downloads
              widget: text
            - label: careers
              name: careers
              widget: text
            - label: gallery
              name: gallery
              widget: text
            - label: feedback
              name: feedback
              widget: text
            - label: policies
              name: policies
              widget: text
            - label: termsconditions
              name: termsconditions
              widget: text
            - label: help
              name: help
              widget: text
            - label: webmanager
              name: webmanager
              widget: text
            - label: sitemap
              name: sitemap
              widget: text
            - label: disclaimer
              name: disclaimer
              widget: text
            - label: date1
              name: date1
              widget: text
            - label: monthyear1
              name: monthyear1
              widget: text
            - label: announcement1
              name: announcement1
              widget: text
            - label: date2
              name: date2
              widget: text
            - label: monthyear2
              name: monthyear2
              widget: text
            - label: announcement2
              name: announcement2
              widget: text
            - label: date3
              name: date3
              widget: text
            - label: monthyear3
              name: monthyear3
              widget: text
            - label: announcement3
              name: announcement3
              widget: text
            - label: circular1
              name: circular1
              widget: text
            - label: news1
              name: news1
              widget: text
            - label: downloads1
              name: downloads1
              widget: text
            - label: careers1
              name: careers1
              widget: text
            - label: contentmanager
              name: contentmanager
              widget: text
            - label: designedby
              name: designedby
              widget: text
            - label: designer
              name: designer
              widget: text
            - label: updation
              name: updation
              widget: text
            - label: dateupdated
              name: dateupdated
              widget: text
              hint: This value is NOT what controls the footer's actual displayed date anymore — use the Site Settings collection for that. This key is effectively unused now; left here only so nothing in the translation file goes unmapped.
      - name: "strings_ta"
        label: "All Site Text (Tamil)"
        file: "languages/ta.json"
        description: "The Tamil translation for every key in 'All Site Text (English)' above. Field order matches exactly; each field's hint shows the current English text for that key so you can translate it without switching tabs. Keep every key present in both files — a key missing from this file will show blank when a visitor switches the site to Tamil."
        fields:
            - label: home
              name: home
              widget: text
              hint: 'EN: Home'
            - label: government
              name: government
              widget: text
              hint: 'EN: Government of Tamil Nadu'
            - label: scert
              name: scert
              widget: text
              hint: 'EN: SCERT Tamil Nadu'
            - label: address
              name: address
              widget: text
              hint: 'EN: (A government educational institution under the Department of School Education, Government of Tamil Nadu)'
            - label: skip
              name: skip
              widget: text
              hint: 'EN: Skip to main content'
            - label: search
              name: search
              widget: text
              hint: 'EN: Search'
            - label: web
              name: web
              widget: text
              hint: 'EN: The Web'
            - label: diet
              name: diet
              widget: text
              hint: 'EN: DIET, Chennai'
            - label: more
              name: more
              widget: text
              hint: 'EN: More'
            - label: aboutus
              name: aboutus
              widget: text
              hint: 'EN: About Us'
            - label: aboutdiet
              name: aboutdiet
              widget: text
              hint: 'EN: About DIET'
            - label: introduction
              name: introduction
              widget: text
              hint: 'EN: About DIET Chennai'
            - label: missionvision
              name: missionvision
              widget: text
              hint: 'EN: Mission and Vision'
            - label: rolefunction
              name: rolefunction
              widget: text
              hint: 'EN: Roles and Functions'
            - label: organization
              name: organization
              widget: text
              hint: 'EN: Organization Structure'
            - label: districtprofile
              name: districtprofile
              widget: text
              hint: 'EN: District Profile'
            - label: districtstatistics
              name: districtstatistics
              widget: text
              hint: 'EN: District Statistics'
            - label: principalsdesk
              name: principalsdesk
              widget: text
              hint: 'EN: Principal''s Desk'
            - label: staff
              name: staff
              widget: text
              hint: 'EN: Faculty and Staff'
            - label: academicstaff
              name: academicstaff
              widget: text
              hint: 'EN: Academic Faculty'
            - label: adminstaff
              name: adminstaff
              widget: text
              hint: 'EN: Administrative Staff'
            - label: courses
              name: courses
              widget: text
              hint: 'EN: Courses'
            - label: coursedeled
              name: coursedeled
              widget: text
              hint: 'EN: Diploma in Elementary Education (D.El.Ed.,)'
            - label: coursetpd
              name: coursetpd
              widget: text
              hint: 'EN: Teachers'' Professional Development (TPD)'
            - label: departments
              name: departments
              widget: text
              hint: 'EN: Departments'
            - label: pste
              name: pste
              widget: text
              hint: 'EN: Pre-service Teacher Education'
            - label: tpd
              name: tpd
              widget: text
              hint: 'EN: Teacher Professional Development'
            - label: etmd
              name: etmd
              widget: text
              hint: 'EN: Education Technology & Material Development'
            - label: ae
              name: ae
              widget: text
              hint: 'EN: Assessment & Evaluation'
            - label: fiar
              name: fiar
              widget: text
              hint: 'EN: Field Interaction & Action Research'
            - label: library
              name: library
              widget: text
              hint: 'EN: Library'
            - label: actsrules
              name: actsrules
              widget: text
              hint: 'EN: Acts and Rules'
            - label: rti
              name: rti
              widget: text
              hint: 'EN: Right to Information Act'
            - label: dietrules
              name: dietrules
              widget: text
              hint: 'EN: DIET Acts & Rules'
            - label: activities
              name: activities
              widget: text
              hint: 'EN: Activities'
            - label: calendar
              name: calendar
              widget: text
              hint: 'EN: Academic Training Calendar'
            - label: current_activities
              name: current_activities
              widget: text
              hint: 'EN: Current Activities'
            - label: activity_archive
              name: activity_archive
              widget: text
              hint: 'EN: Activity Archive'
            - label: alumni
              name: alumni
              widget: text
              hint: 'EN: Alumni Association'
            - label: textbookmodules
              name: textbookmodules
              widget: text
              hint: 'EN: Textbook and Modules'
            - label: textbook
              name: textbook
              widget: text
              hint: 'EN: Textbooks'
            - label: modules
              name: modules
              widget: text
              hint: 'EN: Modules'
            - label: contact-us
              name: contact-us
              widget: text
              hint: 'EN: Contact Us'
            - label: documents
              name: documents
              widget: text
              hint: 'EN: Documents'
            - label: publications
              name: publications
              widget: text
              hint: 'EN: Publications'
            - label: reports
              name: reports
              widget: text
              hint: 'EN: Reports'
            - label: forms
              name: forms
              widget: text
              hint: 'EN: Forms'
            - label: important_links
              name: important_links
              widget: text
              hint: 'EN: Important Links'
            - label: dietfullname
              name: dietfullname
              widget: text
              hint: 'EN: District Institute of Education and Training (DIET), Chennai'
            - label: readmore
              name: readmore
              widget: text
              hint: 'EN: Read More '
            - label: principalname
              name: principalname
              widget: text
              hint: 'EN: Dr. S. Shameem'
              # NOT read anywhere anymore \u2014 use the Principal collection's Name (Tamil) field instead.
            - label: principaldesignation
              name: principaldesignation
              widget: text
              hint: 'EN: Principal, DIET Chennai'
              # NOT read anywhere anymore \u2014 use the Principal collection's Designation (full title, Tamil) field instead.
            - label: announcements
              name: announcements
              widget: text
              hint: 'EN: Announcements'
            - label: circulars
              name: circulars
              widget: text
              hint: 'EN: Circulars'
            - label: media
              name: media
              widget: text
              hint: 'EN: News'
            - label: downloads
              name: downloads
              widget: text
              hint: 'EN: Downloads'
            - label: careers
              name: careers
              widget: text
              hint: 'EN: Careers'
            - label: gallery
              name: gallery
              widget: text
              hint: 'EN: Gallery'
            - label: feedback
              name: feedback
              widget: text
              hint: 'EN: Feedback'
            - label: policies
              name: policies
              widget: text
              hint: 'EN: Website Policies'
            - label: termsconditions
              name: termsconditions
              widget: text
              hint: 'EN: Terms and Conditions'
            - label: help
              name: help
              widget: text
              hint: 'EN: Help'
            - label: webmanager
              name: webmanager
              widget: text
              hint: 'EN: Web Information Manager'
            - label: sitemap
              name: sitemap
              widget: text
              hint: 'EN: Sitemap'
            - label: disclaimer
              name: disclaimer
              widget: text
              hint: 'EN: Disclaimer'
            - label: date1
              name: date1
              widget: text
              hint: 'EN: 26'
            - label: monthyear1
              name: monthyear1
              widget: text
              hint: 'EN: FEB 2025'
            - label: announcement1
              name: announcement1
              widget: text
              hint: 'EN: Prevention of child sexual abuse: In-service teacher training for primary teachers'
            - label: date2
              name: date2
              widget: text
              hint: 'EN: 12'
            - label: monthyear2
              name: monthyear2
              widget: text
              hint: 'EN: FEB 2025'
            - label: announcement2
              name: announcement2
              widget: text
              hint: 'EN: Educational video creation for Manarkeni app.'
            - label: date3
              name: date3
              widget: text
              hint: 'EN: 21'
            - label: monthyear3
              name: monthyear3
              widget: text
              hint: 'EN: JAN 2025'
            - label: announcement3
              name: announcement3
              widget: text
              hint: 'EN: Conduct of State Leve Achievement Survey (SLAS) in Chennai District'
            - label: circular1
              name: circular1
              widget: text
              hint: 'EN: Circular on NCTE Draft regulations 2025'
            - label: news1
              name: news1
              widget: text
              hint: 'EN: To be updated soon'
            - label: downloads1
              name: downloads1
              widget: text
              hint: 'EN: Research compendium: Chennai district (2023-24)'
            - label: careers1
              name: careers1
              widget: text
              hint: 'EN: To be updated soon'
            - label: contentmanager
              name: contentmanager
              widget: text
              hint: 'EN: Website Content Managed by'
            - label: designedby
              name: designedby
              widget: text
              hint: 'EN: Developed by'
            - label: designer
              name: designer
              widget: text
              hint: 'EN: Ravikumar Rajabhather'
            - label: updation
              name: updation
              widget: text
              hint: 'EN: Last Updated on: '
            - label: dateupdated
              name: dateupdated
              widget: text
              hint: 'EN: 21-JUNE-2025'
              # Not what controls the footer's actual date; see the note on the English side.

  - name: "pages"
    label: "All Website Pages (page source)"
    delete: false # Prevents deleting core website structure
    files:
      - name: "404"
        label: "404 (raw HTML)"
        file: "src/404.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "about-diet-chennai"
        label: "About Diet Chennai (raw HTML)"
        file: "src/about-diet-chennai/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "about-diet"
        label: "About Diet (raw HTML)"
        file: "src/about-diet/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "academic-faculty"
        label: "Academic Faculty (raw HTML)"
        file: "src/academic-faculty/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "activ-alumni"
        label: "Activ Alumni (raw HTML)"
        file: "src/activ-alumni/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "activ-archive"
        label: "Activ Archive (raw HTML)"
        file: "src/activ-archive/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "activ-calendar"
        label: "Activ Calendar (raw HTML)"
        file: "src/activ-calendar/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "activ-current"
        label: "Activ Current (raw HTML)"
        file: "src/activ-current/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "admin-staff"
        label: "Admin Staff (raw HTML)"
        file: "src/admin-staff/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "announcements"
        label: "Announcements (raw HTML)"
        file: "src/announcements/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "assets-content-about-mission"
        label: "Assets / Content / About Mission (raw HTML)"
        file: "assets/content/about_mission.html"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits the ENTIRE raw HTML file, including navigation and footer. Use with care — for announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "assets-content-about-roles"
        label: "Assets / Content / About Roles (raw HTML)"
        file: "assets/content/about_roles.html"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits the ENTIRE raw HTML file, including navigation and footer. Use with care — for announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "assets-content-statistics"
        label: "Assets / Content / Statistics (raw HTML)"
        file: "assets/content/statistics/index.html"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits the ENTIRE raw HTML file, including navigation and footer. Use with care — for announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "assets-docs-mandatory-disclosure"
        label: "Assets / Docs / Mandatory Disclosure (raw HTML)"
        file: "assets/docs/mandatory_disclosure.html"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits the ENTIRE raw HTML file, including navigation and footer. Use with care — for announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "circulars"
        label: "Circulars (raw HTML)"
        file: "src/circulars/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "contact-us"
        label: "Contact Us (raw HTML)"
        file: "src/contact-us/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "coursedeled"
        label: "Coursedeled (raw HTML)"
        file: "src/coursedeled/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "courses"
        label: "Courses (raw HTML)"
        file: "src/courses/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "coursetpd"
        label: "Coursetpd (raw HTML)"
        file: "src/coursetpd/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "default"
        label: "Default (raw HTML)"
        file: "default.html"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits the ENTIRE raw HTML file, including navigation and footer. Use with care — for announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "departments"
        label: "Departments (raw HTML)"
        file: "src/departments/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "dept-ae"
        label: "Dept Ae (raw HTML)"
        file: "src/dept-ae/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "dept-etmd"
        label: "Dept Etmd (raw HTML)"
        file: "src/dept-etmd/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "dept-fiar"
        label: "Dept Fiar (raw HTML)"
        file: "src/dept-fiar/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "dept-pste"
        label: "Dept Pste (raw HTML)"
        file: "src/dept-pste/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "dept-tpd"
        label: "Dept Tpd (raw HTML)"
        file: "src/dept-tpd/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "disclaimer"
        label: "Disclaimer (raw HTML)"
        file: "src/disclaimer/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "district-statistics"
        label: "District Statistics (raw HTML)"
        file: "src/district-statistics/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "district-profile"
        label: "District Profile (raw HTML)"
        file: "src/district_profile/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "doc-forms"
        label: "Doc Forms (raw HTML)"
        file: "src/doc-forms/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "doc-publications"
        label: "Doc Publications (raw HTML)"
        file: "src/doc-publications/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "reports"
        label: "Reports (page source)"
        file: "src/reports/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "downloads"
        label: "Downloads (raw HTML)"
        file: "src/downloads/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "feedback"
        label: "Feedback (raw HTML)"
        file: "src/feedback/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "gallery"
        label: "Gallery (raw HTML)"
        file: "src/gallery/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "help"
        label: "Help (raw HTML)"
        file: "src/help/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "important-links"
        label: "Important Links (raw HTML)"
        file: "src/important-links/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "index"
        label: "Index (raw HTML)"
        file: "src/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "library"
        label: "Library (raw HTML)"
        file: "src/library/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "media"
        label: "Media (raw HTML)"
        file: "src/media/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "mission-vision"
        label: "Mission Vision (raw HTML)"
        file: "src/mission-vision/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "org-structure"
        label: "Org Structure (raw HTML)"
        file: "src/org-structure/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "principals-desk"
        label: "Principals Desk (raw HTML)"
        file: "src/principals-desk/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "roles-functions"
        label: "Roles Functions (raw HTML)"
        file: "src/roles-functions/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "rti-diet-rules"
        label: "Rti Diet Rules (raw HTML)"
        file: "src/rti-diet-rules/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "rti"
        label: "Rti (raw HTML)"
        file: "src/rti/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "sitemap"
        label: "Sitemap (raw HTML)"
        file: "src/sitemap/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "tb-module"
        label: "Tb Module (raw HTML)"
        file: "src/tb-module/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "tb-textbook"
        label: "Tb Textbook (raw HTML)"
        file: "src/tb-textbook/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "terms-conditions"
        label: "Terms Conditions (raw HTML)"
        file: "src/terms-conditions/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "theme-css-site-change"
        label: "Theme / Css / Site Change (raw HTML)"
        file: "theme/css/site-change.html"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits the ENTIRE raw HTML file, including navigation and footer. Use with care — for announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "web-manager"
        label: "Web Manager (raw HTML)"
        file: "src/web-manager/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."
      - name: "website-policies"
        label: "Website Policies (raw HTML)"
        file: "src/website-policies/index.njk"
        fields:
          - label: "Full page HTML source"
            name: "body"
            widget: "code"
            default_language: "html"
            allow_language_selection: false
            output_code_only: true
            hint: "This edits this page's content and metadata (title, description, last-updated date). The shared navigation, header, and footer are no longer part of this file (Phase 2 migration) — they live once in src/_includes/ and apply to every page automatically. For announcements, circulars, downloads, banners, and the last-updated date, use the dedicated sections above instead."